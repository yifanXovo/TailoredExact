# Historical context (not fresh Round 20 evidence)

Round 19 established that one persistent global Gini tree could preserve exact
certificates and improve several cases, but it regressed markedly on
`tight_T_seed3101` and `high_imbalance_seed3202` relative to the legacy
standalone-interval process. Those historical results motivate the Round 20
questions only. They are deliberately excluded from every fresh numerical
table and from arm selection.
