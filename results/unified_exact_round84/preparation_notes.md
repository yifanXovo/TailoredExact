# Reader preparation while the original queue runs

Only lightweight source/metadata reads, text edits and Python syntax compilation
were performed alongside the first native run. No optimizer, build, full audit,
archive compression, plot or new data generation was launched by this preparation.

round84_analyze.py and round84_mechanism.py reuse the qualified R83 readers.
Their initial textual derivation is retained in offline_reader_derivation.json.
Additional pre-result checks bind all nine identities, current CMake and the
three archived startup initial/final route hashes, besides all25 logical paths.
Final script bytes are recorded separately in offline_preparation.json.

round84_replication.py reuses the existing frozen comparison and identical-pair
checks. It retains the three U6 outcomes from both stages and eighteen matched
checkpoints, then compares each algorithm pair within its own realization.
It does not import a previous P/K1 outcome into a current comparison.

round84_package.py reuses the established per-member byte verifier. It packages
only nine new whole runs and three reference exports, binding the already
published qualification/source/startup bundles. round84_plot.py renders all
three current roles and preserves missing physical U as a blank U/gap.

Two read-only discovery commands failed: Windows rg did not expand the literal
scripts/round81* and scripts/round80* arguments; a guessed round82_prepare.py
path did not exist. File discovery was corrected using rg --files and --glob;
the actual generator is round82_generate.py. These were metadata reads, with
zero optimizer calls and no changed experimental state. A transient plus sign
in the new recovery prose was removed before committing. None required a
native retry or changed the frozen experiment.
