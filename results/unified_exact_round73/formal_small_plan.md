# First full-proof screen after native persistence qualification

Hypothesis: the added constructive seed improves the complete exact process,
not merely startup U, on the two small roles with the strongest revised
initial-U change. Test D3 and D4, in that order, with fresh P-GRB, DS-X and
JDS-X in that order on each role. This is an exposed development panel.
All six runs use one whole-process300s cap, native work ends against the
same absolute deadline with3s shutdown reserve, and the supervisor may
terminate the entire owned child at298s with2s left for kill/exit. No branch
or algorithm is resumed after this stop. Any observed wall>300s remains a
failed attempt. Maximum6 fresh process launches/1800s; no retry is opened.

Use qualified v6 source4ba09609cf3fb92ef29598dce8201231668324f9 and identical
read-only journal instrumentation on all arms. P stays original compact,
native defaults, Threads1/Seed0/PresolveAuto, requested gaps0 and original
numeric tolerances, with no Start/cuts/imported bounds. DS-X is
research-round71-vds-interroute-descent; JDS-X is
research-round73-vds-joint-seeded-descent. Both use the same VD-S/AM exact
proof after their paid finite startup. All24/25 seeds and construction costs,
LP probes, model work, native calls, observer writes and exits are charged.

Reuse the just-qualified v6 D3 reference fingerprint and input hash; perform
one new D4 build-only reference export/read, at most30s and zero Optimize.
Freeze complete commands, input hashes, binary/source identities and script
hashes before each launch. Logical2/mask4 affinity and restoration apply to
the owned launcher and child. Do not touch other processes.

Normal endpoints require final original physical witnesses, original
certificate gates and complete frontier evidence. A hard-stop endpoint may
use only complete, observed-by-cutoff physical/full-domain journal records,
explicitly interrupted and uncertified; absent evidence rejects it. Reject
bound/witness contradictions, never clip. Any validity failure stops the
unlaunched remainder. Preserve all attempts and costs. Offline replay is
separately charged. Compare complete wall/certification and U/L/gap together
using the existing frozen material/severe criteria from R69; do not claim
significance from an isolated difference.

C2's startup is unchanged and is deferred, not declared a strict new-build
tie. D6 startup quality was protected in the two paired startup tranches,
but full v6 proof performance is still unknown. D7 remains a major K1
protection deficit until full evidence says otherwise. This plan opens no
medium/long/confirmation panel. Choose the next bounded decisive panel from
these results; do not close R73 as an instrumentation-only stage.
