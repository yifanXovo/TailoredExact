# Composite-penalty endpoint qualification and conditional probe admission

Astra has read the six-test oracle log, outside-command receipt, adapter contract and independent static review, and independently matched the final oracle/adapter/test hashes. The oracle's six tests pass, including 216 independent inventory enumerations; qualification cost is 0.2646866 seconds. This is not evidence of practical improvement.

Freeze the composite oracle SHA256 `dccbf59fa462a25308419f49a719f59d0d9ef514f917ab377b9215ffdcdcd898`, its test `932cc1f4a47c25a8c08195ce8d4a0208062eb6f361ff36a892c5f394d213149e`, the separate endpoint adapter `7ded4c4ce60bf075be0f373c38a7912e7d915cae658407307c9887c87597c452`, and its test `804f4ac9e14f9945cc1972285475bef6a250c9a45e4841029f1ec0e3e547a469`. Preserve all frozen original flow/probe modules, C++, input/witness identities and numerical conventions.

The startup worker may acquire the exclusive computation slot only after the OT worker explicitly reports that its batch has ended, no descendants remain, and the slot is released. Finish the bounded read-only B3 note first if necessary. Do not infer release from silence, a missing process sample or a timeout of the coordinator's wait.

After release, authorize the six pure adapter tests once and one E8 baseline-import qualification with the already-qualified `round88_quantity_import_qualify.py`, both with complete original logs and outside launch-to-exit receipts in a new `qualification/a2_composite_endpoint_20260927` directory. This checks the reused native boundary; it is not an additional optimization attempt. The existing real Windows Job cleanup test is reusable because that implementation is unchanged. Do not rerun the passed oracle suite or add unrelated tests.

If and only if all six adapter tests pass, the one E8 native receipt is valid, the expected source/binary/input/witness identities remain unchanged, and the slot is still exclusively held, the same worker is authorized to run the three fixed offline cases once in order D6, E8, S12. Use the new adapter's `supervise` entry and fresh directories in `a2_composite_endpoint_diagnostic_001`, each with its frozen 120-second external whole-case process-tree deadline. This includes preflight, all flow computations, every full primitive integer line, all original-C++ calls and evidence serialization. No internal time/Work/round/point quota or transition to another algorithm is introduced. Continue only through the adapter's declared mathematical conditions and original-F strict acceptance.

Record qualification and all case costs separately, complete original receipts, full flow certificates, checkpointed partial work, actual original F/G/P, accepted inventories, termination state and memory-observation limitations. A normal whole-case timeout is an unknown research result and permits the next independent case. Identity/correctness failure, unexpected native receipt or crash, and resource failure stop the batch. Preserve the paid attempt; do not silently repair/retry, alter the frozen implementation, change tolerances or add instances. Routine sampling remains inside the worker. No ENS-C integration or mainline promotion is authorized. If the fixed three points offer no original-F improvement, park this quantity-flow direction under the accepted decision instead of extending variants.

On completion or a stop condition, write the qualification and diagnostic reports, verify no descendants remain, and release the computation slot explicitly. Root alone owns Git mutations. The xhigh reviewer will independently check the recorded executable qualification and three-case evidence before any next-stage decision.
