# Round84 uses the unchanged complete ENS-C method

The algorithm, numerical semantics, parameters, applicability guard and source
are exactly those in ../unified_exact_round83/unified_method.md. The inventory
and route-prefix proof for the added operator is in that stage's mathematics.md;
its original pre-integration wording describes the proposal's status then.
Subsequent R83 qualification and complete-method audits establish the implemented
integration. R84 introduces no new algorithm or correctness claim.

One default-off preset remains research-round83-vds-equal-net-exchange. It
combines finite25-path decoded startup, strict physical closure and the union
of balanced relocation/equal-net block exchange, then the inherited complete
VD-P/F0/AM proof and verified native Start path. Every step uses the same
whole-process deadline. There is no internal component resource allocation,
input-name dispatch, archived route import or choice of the best full algorithm.

The equal-net move retains each station's integer operation and original F,
preserves outside-block prefixes and loaded returns, and checks the recipient's
actual capacity and both full route durations. It is a feasible-witness mechanism;
complete domain coverage and original global bounds still come from the exact
method. The complete method retains the symmetric metric/nonnegative-penalty
guard of its strengthening. Isolated directed/nonmetric operator tests do not
broaden the whole method's input scope.

P-GRB uses only the original compact model and native defaults. K1-R uses
research-round65-k1-h with the same reliability repairs. All three arms bind
the identical qualified R83 executable. R84 measures long protection and one
finite U6 repeat, with all original costs and U/L directions preserved. It
cannot establish a new theorem, statistical equivalence or overall generalization.
