# Offline scope check after the negative startup screen

All ten admitted startup runs returned normally. QDS-X finds no strict move
in all five roles; each final route hash equals its JDS-X control. Before
closing this negative screen, independently enumerate the full declared
single/pair integer neighborhood of the saved D6 and D7 physical endpoints.
This checks the production feasibility count and lack of improving F on the
actual larger states, complementing the51 synthetic oracle comparisons.

Admit exactly two offline Python oracles, each at most30s, total60s, zero
Optimize and no new solver/heuristic run. Parse each frozen original instance
once; enumerate full station operation domains, rebuild every candidate route
including zero deletion, and check every physical prefix/duration and the full
quadratic Gini formula. Do not reuse C++ load ranges or incremental objective.
Saved endpoints enter only this diagnostic, never the running algorithm.
Retain timeout/failure if either audit does not finish. No automatic rerun or
additional qualification/full-performance/confirmation allocation follows.
