# Bounded cut-development history

## Iteration 1 — SD-R3-ROOT-BLOCKMAX

The plan and its hash were frozen before candidate runs. One development-only revision corrected generation-order dependence in exact dominance removal before the formal core panel; its separate plan/hash were frozen first. The post-revision implementation passed 44 dedicated cases and a forced real `GRBcbcut` fixture.

The 120-second D1–D14 core completed with zero correctness, false-certificate, callback, vector-read, or submission failures. F3 generated 852,570 candidates, found two strictly violated rows, selected and added both, and spent 5.50294 seconds in the tailored callback. It preserved the F0 certificate set, but the common-exact Work geometric mean ratio was 0.9812417355 rather than the required 0.97; no additional certificate was gained.

The predeclared correctness gate opened the 300-second complete development panel. F0 certified 11 states; F3 certified nine. F3 lost D1 and D13, gained none, and had a common-exact Work geometric mean ratio of 0.9776120499. D13 generated no submitted cut, so its loss implicates the required dynamic callback/`PreCrush` path rather than a particular row. F3 was rejected and did not enter sealed confirmation.

The controlled static ablation was also negative. F1 and F2 both lost D10’s baseline certificate; F2 additionally received a strict certificate rejection on D12. Production v0 lost D12 on the 120-second screen because of its historical exhaustive small-instance subset block. These results document static congestion but do not authorize F0 as the final backend under the Round 52 contract.

## Iteration 2 — formally skipped

Support-rank, tree-scope, Pareto-selection, and static/dynamic hierarchy variants were considered. Every permitted dynamic candidate retains the callback/`PreCrush` path already associated with the zero-cut D13 certificate loss. The more expansive choices also weakly increase generation or submission volume. With two severe lost certificates and no qualifying aggregate gate, no candidate run was opened.

## Iteration 3 — formally skipped

No Iteration 2 candidate survived, and no second family had a qualifying offline violation census capable of repairing the shared infrastructure-path regression. The round terminated with the bounded systematic negative result; no witness-specific dispatch, fitted weight, or post hoc family was introduced.
