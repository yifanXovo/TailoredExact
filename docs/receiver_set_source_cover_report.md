# Receiver-Set Source-Cover Report

`--compact-bc-receiver-source-cover singleton-paper-safe` enables only the singleton net-delivery row proved in `receiver_set_source_cover_proof.md`. The moderate_seed3301 receiver diagnostic added nine receiver rows in the interval children but did not certify the row at the short smoke budget.

Modes other than singleton paper-safe are diagnostic/off for paper evidence in this round.
