# Preparation and recovery record

Before admission, a read tried results/unified_exact_round79/protocol.json,
which does not exist. The actual R79 driver reads the R71 protocol; its saved
campaign/identity.json supplies the concrete launched roles and is used here.
The failed read made no changes and launched no solver. A following rg command
used literal Windows wildcard path arguments and returned a path error; the
correct --glob search succeeded. Neither is performance evidence.

R84 final publication is independently verified at exact head ed0cd977ac2ea1cf7a7bf1236a15dd845a3ed948.
Its final_metadata.json transport receipt is retained and bound in R85, avoiding
self-referential metadata commits or any rewrite of the R84 branch/PR.

The initial staged whitespace check flagged CRLF endings in generated Python
files preserved with -text. The PowerShell command sequence continued to the
initial commit despite that native command's nonzero exit. No solver had been
launched. A separate follow-up declares whitespace=cr-at-eol for R85 scripts,
preserving their measured bytes, and checks the entire stage diff again before
launch. Subsequent command sequences explicitly gate on native exit codes.

One bounded ordinary preparation push failed with an empty server reply,
Git128 after5.2738275s; initial_push_attempt.json retains the original receipt.
The unchanged R84 UTF-8-only exact-tree publisher is adapted only for this
stage branch/base/output paths. It verifies exact original tree/commit IDs
and uses a non-force update; no evidence history is rewritten.
