# Round89 status

Same-checkout branch: `codex/round89-ensc-native-ot-b1`, based on the final Round88 documentation correction `cce067956156b505652110f5dde6839c3af8c4e1`. The user-authorized overall goal remains active and unfinished.

The default-off B1 native callback prototype compiles and passes the final independent xhigh static review after targeted repairs. Draft PR151 is attached: https://github.com/yifanXovo/TailoredExact/pull/151 . Qualification001 passes the two existing regressions but stops at a fixture-bound error, with zero Optimize. Qualification002 corrects only those two test bounds, passes the pure and 54-point exact-dyadic oracle, and completes six toy Optimize arms. No MIPNODE event or cut submission occurs, so callback qualification remains inapplicable. Costs are 3.751s and 3.861s outer wrapper wall. Both failed/partial batches and independent reviews are preserved.

One replacement parity toy with analytic integer optimum1 and LP value0 is in test-only preparation and independent review. Per-arm direct primals will persist before assertions; backend MIP raw primals are unavailable through the frozen interface and that limitation is explicit. No additional Optimize or real screen is admitted yet. A conditional eight-instance/two-arm runner is source-only preparation, including a correction for old model files overwritten on a legitimate incumbent epoch. Production source/main binary remain unchanged. Root alone writes Git.

Frozen candidate identities are in `static_review_handoff.md`. Detailed compile logs are preserved both at their original build paths and in `compile_logs/`; the original files are unchanged. No historical binary, raw evidence, unrelated untracked tree or legacy dirty result file is modified by this stage.

Native OT has no performance evidence yet. There is no mainline promotion or broad long-run campaign. The separately reviewed LP-G split remains a possible later single-factor ablation, retaining the old AM gate and finite-depth safeguard; it is not mixed into this prototype.
