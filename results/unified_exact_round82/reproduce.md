# Round82 evidence and reproduction

All18 original arms are closed, normal and valid; driver78681 exits0. Joint
and actual-mechanism audits pass,19 bundles verify twice, and the final figure
passes visual review. Never rerun the original driver or completed producers.
The U6 performance failure remains; overall goal is unmet. Publication pending.

## Frozen identities and prospective generation

The owned checkout is E:/codes/ExactEBRP-round66 on
codex/round82-bdsc-unadapted-confirmation. Its base is R81 final
298e97f5d163a54eb9d899c664cafd9f07c8647c / draft PR142. The separate original
dirty checkout is not used or altered. Pre-generation commit
d7e5d5fd64ffffc1694d22cba8f1490cc19de1a6 fixes plan.md, preregistration.json and
scripts/round82_generate.py. Subsequent commit
c58797a610e7a4e3bfbeb702f702849dfdb7c51d binds all six generated input files
and the serial driver before any formal launch. Both are preserved on remote.

reference/round82_unadapted_confirmation contains the exact serialized inputs,
generation records and Citi selection/landscape metadata. protocol.json binds
each input SHA256 and mathematical scenario; generation.json binds generator
hashes, the prior freeze commit, nonzero necessary conditions and geographic
overlap. There were no rejected or replacement draws. Generation is data-only;
its guarded producer refuses to overwrite the existing namespace. For an
independent generation replay, use a separate checkout at the pre-generation
commit and the same authoritative Citi source files. This regeneration has
not been rerun in this stage and is not counted as extra confirmation.

The local source paths used by the inherited Citi loader are
E:/codes/Hybrid GA/testdata/CitiBike/coords/citibike_selected_443_capacity_list.txt
and citibike_selected_443_coords_utm18n_meters.txt. Their SHA256 values are
92ca9b05e66d1425bbc801be2dd9c0776b1a9707e62a60b821d941aa98625ae1 and
c5fd892cc82e07332d4796edb0527182d9c60883acc9b87a5410dab569f990d1.
Older companion station annotations are read-only metadata, not new demands.

## Measured code and execution

The production source is unchanged R78 commit
4a0561e0f8193e3e874c1bd0bfa8bc381fb66f5e. Local executable
build/round78/v1/ExactEBRP.exe SHA256 is
3fae847a75c3c9d07d8f5f0444daeafb73559865e6bffb816746802b9e2f43ab.
The executable and Gurobi licence are not committed. R78 qualification evidence
records60 passing tests and147 native calls; this stage does not rerun them.
full_preflight.json records fresh source/binary verification and six new
zero-Optimize original compact reference exports. New inputs correctly have
new fingerprints instead of an invented match to historical benchmark models.

campaign/identity.json contains every exact command, input/model binding,
binary/source/driver/reader hash and cap. campaign/processes.jsonl is the
write-ahead attempt ledger. Per-arm launch, completion, affinity, result,
observations and audit files live under campaign/local_raw. There is one
optimizer process at a time, on logical2/mask4, with restoration checked.
Use Gurobi13.0.2 and the pinned solver settings, numerical tolerances and sole
whole-run deadline in plan.md. No private internal time/Work allocation.

The three configurations are official P-GRB --plain-baseline,
research-round78-vds-balanced-descent and research-round65-k1-h. Both research
arms use the same legal-witness and zero-stop reliability repairs. The complete
method is R81/unified_method.md; R82 changes no method or parameter.

The original driver is scripts/round82_research.py, launched once with
D:/msys64/ucrt64/bin/python.exe (Python3.12.7, GCC UCRT14.2.0) and PYTHONUTF8=1.
runtime_environment.json records a readback during this same campaign. Gurobi
uses Seed0. The inherited heuristic RNG seed is20260626 for BDS-C and K1-R
(include/Instance.hpp and src/main.cpp feed it into HgaTgbcRunner.cpp). BDS-C
uses24 random initial orders plus the constructive25th order; these pathway
indices and the independent data-generation seeds are distinct from Gurobi Seed.
No seed was changed or searched in this stage. It refuses to reuse campaign/.
Do not restart it or rerun completed arms. A deliberate replication requires
a separately admitted namespace and current controls, not removal of guards
or selecting the best of repeats. No replication is admitted here.

## Offline evidence review after the queue closes

scripts/round82_analyze.py replays observed committed receipts, independent
physical witnesses, bound scopes, final endpoints, actual-availability
checkpoints, fixed comparisons and cross-arm bound consistency. All final
source/input hashes are checked. No pooled U/L becomes a performance endpoint.
scripts/round82_mechanism.py checks current startup paths, balanced/strict
transitions, outer handoff, actual submitted vectors, model rows and readback.
New roles have no prior-route equality test. Existing routes are never inputs.

Both audit producers refuse an active campaign or an existing final audit.
They are not timing benchmarks or additional Optimize calls. Raw signed gaps,
including numerical values near zero, are preserved. Whole-deadline hard stops
can retain committed physical U/global L under the frozen policy, but cannot
be relabeled as normal results or certificates. Unseen or incomplete payloads
remain raw evidence and are not promoted to earlier checkpoints.

scripts/round82_package.py creates one lossless archive per arm plus the six
reference trees, then verifies every archived byte against the original path,
size and SHA256. bundle_manifest.json is the compact index;
bundle_members.json.gz contains all member records. After packaging, a
read-only byte check can be repeated with:

    D:/msys64/ucrt64/bin/python.exe scripts/round82_package.py --verify-only

The checked figure producer is scripts/round82_plot.py. After the two audits
pass it reads only their long-window observations. It uses the existing
build/round81/plot_env/Scripts/python.exe environment, whose exact packages
are recorded in R81/plot_requirements.txt. No new installation or optimizer is
required. It refuses to overwrite figures/. The first rendering is preserved in figures/ with its legend-overlap review.
A separate scripts/round82_plot_v2.py writes figures_v2/ with a shared external
legend and passes visual review. Both renders have identical plotted-checkpoint
bytes, recorded hashes and costs. Dashed lines guide the eye; missing physical
U is not a zero gap.

Absolute Windows model/log paths are retained in raw journal receipts. Byte
verification is portable, but full mathematical replay may need the original
checkout layout or an explicitly recorded path adaptation in a separate copy.
No cross-OS binary reproduction has been validated. Inherited qualification
and full method records remain in earlier commits; they are referenced rather
than copied and counted as new runs.

## Costs, failures and limitations

Paid process wall includes startup, construction, all native work, persistence,
verification and exit. Separate generation, reference/preflight, offline audit,
packaging/plot and publication costs are retained. No cost is subtracted from
formal endpoints. Interactive work is not a complete machine-time census.

initial_push.json and initial_push_http11.json preserve two HTTPS connection
resets. publication_api/initial_recovery.json verifies the exact original
blobs/trees/author/committer/messages/commit hashes, then a non-force ref update
and fresh readback. Later ledgers use distinct labels. This is publication
recovery, not replacement experimental evidence.

The six draws use familiar generator recipes and partly overlapping CitiBike
stations. See scope_and_nonzero.md; confirmation is restricted to this evidence
range. Negative outcomes stay in the table. If used to revise the algorithm,
these data become development for the revision. Final evidence passes, but U6 prevents overall performance acceptance.
The complete mixed result is published as a separate draft; no main merge.
Any subsequent revision informed by R82 must use new unadapted confirmation.
