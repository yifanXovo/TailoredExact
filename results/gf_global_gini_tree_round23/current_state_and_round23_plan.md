# Current state and Round 23 plan

## Frozen entry state

- Local base: `3c69cb2c04889db5ceb40e313a710d600089879d`.
- Observed remote main: `91ed0e314caae51ddbd8375d88cd65a5352c917f`.
- Relationship: different commit history, identical source tree.
- Entry build: clean isolated MinGW release build in `build_round23/`.
- Entry tests: all seven existing C++ executables passed (86 groups); all
  existing Python validation files passed (12 groups/checks).
- Frozen Round 22 executable: present and SHA-256 verified.
- Round 22 official package: immutable and unchanged.

## Scientific plan and gates

1. Independently bind and verify the retained moderate4301 HGA and plain
   witnesses; audit semantic and complete auxiliary root extensions.
2. Locate the first lifecycle loss with retained and targeted diagnostics;
   classify the native status scope and install a fail-closed consistency gate.
3. Apply only a general correction, prove it, and revalidate corrected S0/S1.
4. Recompute retained S0/plain comparisons under independently frozen common
   upper bounds and synthesize the remaining bottleneck.
5. Select at most one mechanism using API feasibility and retained evidence.
   P1 is rejected if generic callbacks cannot safely enumerate/select open
   nodes; otherwise select P2 and prove its estimate before implementation.
6. Commit source, proof, manifests, and evaluation protocol before observing
   official candidate results.
7. Run Stage 0 correctness, dispatch, manifest, exhaustive-toy, and live API
   gates; then serial matched Stage 1 and Stage 2 experiments without
   performance-based cancellation.
8. Produce paired metrics, exactness audit, cautious artifact inventory,
   retention/cleanup audit, final assessment, evidence manifest, and reports.
9. Commit intended evidence, push normally, and verify local, tracking, and
   live remote branch SHAs agree.

## State at protocol freeze

The moderate4301 contradiction is classified as presolve-induced loss of a
successfully created continuous generic-callback child, compounded by
overbroad status scope. Corrected S0 and S1 reach status 108 with valid bounds
and no callback failures. P1 is unavailable in the current generic-callback
architecture; P2, the dispersion-coupled proved child estimate, is selected.

The live P2 smoke additionally exposed an existing metadata guard that rejected
valid CPLEX local-bound contraction after cuts. Equality was replaced by a
fail-closed nested-contraction predicate: contraction is valid, expansion is a
correctness abort. Its regression test passes, and the follow-up smoke passes
all structural gates with 214 children and zero callback/estimate failures.

No official Stage 1 or Stage 2 candidate result has been observed at this
freeze point.
