# Native diagnosis model binding, before launch

v6 passed54/54 tests, including28 journal checks and75 existing native fixture
calls. The six diagnosis roles and limits are unchanged. Before their launch,
run the qualified v6 Round65ReferenceBuild helper exactly once each for tiny
T3/handling0, D6 and D3, in that order. This exports and reads the canonical P
model and native fingerprint with zero Optimize calls. Each has a30s whole
helper safety limit, maximum3 calls/90s; actual wall is separately recorded.
These model/executable identities bind normal P certificates and verify that
the journal option leaves the original compact model unchanged. This is
qualification cost, not performance work, and is never deducted from native
diagnosis wall. Record each exact command before dispatch; retain failures.
