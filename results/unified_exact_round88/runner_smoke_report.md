# Round 88 A1 G3：E8/S12 六臂烟测

预注册的 E8、S12 各三臂已在唯一串行 lease 下完整运行。六臂均从原始输入独立启动，使用同一个冻结 A1 build、相同 Gurobi 配置及 120 秒完整过程上限；均正常返回、审计通过、取得严格原问题最优证书。没有重跑、超时、资源停机或失败尝试。此处只报告两例各一次的烟测证据，不把结果推广为长尾性能或 A1 晋级。

| 例 | 臂 | 物理 U | 有效全局 L | 证书 | 完整进程墙钟 | 含发射前端到端 |
| --- | --- | ---: | ---: | :---: | ---: | ---: |
| E8 | P-GRB | 0.021337006039780566 | 0.021337006039780570 | 是 | 1.891 s | 2.141 s |
| E8 | ENS-C | 0.021337006039780566 | 0.021337006039780573 | 是 | 2.750 s | 3.016 s |
| E8 | A1 | 0.021337006039780566 | 0.021337006039780573 | 是 | 2.765 s | 3.000 s |
| S12 | A1 | 0.058563973125785150 | 0.058563973125784880 | 是 | 1.562 s | 1.828 s |
| S12 | ENS-C | 0.058563973125785150 | 0.058563973125784880 | 是 | 1.953 s | 2.203 s |
| S12 | P-GRB | 0.058563973125785160 | 0.058563973125785120 | 是 | 6.312 s | 6.578 s |

所有端点 gap 仅为浮点舍入量级，E8 的极小负差 `L-U<1e-17` 落在既有 `1e-7` 容差内。六臂 `audit.json` 均为 `passed=true`、最终物理见证的原始 T 可行；原生调用全返回。P-GRB 原始 compact 模型的指纹与规范 SHA 均匹配冻结参考。跨臂对同一原问题汇集历史最大合法全局 L 与所有物理 U：E8 为 `0.021337006039780573 <= 0.021337006039780566 + 1e-7`，S12 为 `0.058563973125785120 <= 0.058563973125785150 + 1e-7`，两份 `runner_cross_arm_*.json` 均 `passed=true`。没有严重退化停批信号。原始 `observations.json`、逐条 receipt/journal、LP、见证、日志与最终审计保存在 `runner_a1_g3/raw/`。

正负两面很清楚：E8 上 A1 与 ENS-C 终点相同，但各比 P 慢约 `0.874 s` 和 `0.859 s`；A1 比 ENS-C 还慢 `0.015 s`。S12 上 A1 比 ENS-C 快 `0.391 s`，且两者都快于 P。这些秒级单次差异不足以证明普遍加速，E8 也未达到预注册的严重退化阈值。D6 的独立启动诊断已表明单种子可能损失物理 UB，故本烟测不能推断长例启动无损。

既有 G3 原始证据支持一个更窄的机制判断：E8/S12 的 A1 与 ENS-C 初始物理 UB 和闭包后的路线哈希分别相同；两例的原生调用数均相同（E8 每臂 5 次，S12 每臂 4 次），对应调用的模型 SHA、全局作用域和 cutoff 全同。逐条 journal 事件 payload 除运行目录内的模型、原生日志路径外一致。E8 的 `exact_phase_start` 为 ENS-C `0.021191 s`、A1 `0.006331 s`，启动提前约 `0.014860 s`，不足以压过后段约同量级的墙钟波动。S12 为 ENS-C `0.421476 s`、A1 `0.013343 s`，提前约 `0.408133 s`，与全过程节省 `0.391 s` 接近。可据此说这两例的 A1 收益主要来自启动成本削减，**不能**仅据相同证据轨迹证明原生搜索耗时逐毫秒相同，也没有观察到更强初始 UB 或更少后段模型。

六臂完整进程墙钟合计 `17.233 s`，发射前 admission/记录合计 `1.533 s`，记录内端到端合计 `18.766 s`；离线物理、闭包与界审计另计 `0.06652 s`，不冒充算法提速。`runner_a1_g3/runner_smoke_completion.json` 载明 `6/6` 通过、失败尝试成本零。无损烟测包 `runner_smoke_raw.tar.gz` 含 campaign 当前 2,271 文件、原始字节合计 6,556,527，压缩包 1,145,934 字节；`runner_smoke_raw_index.json` 给出每文件 SHA256、包 SHA256 和逐文件解包回验。原目录保留，包内不含源码或 build；未来 rest 原始证据须另行封存，不改写本次烟测包。
