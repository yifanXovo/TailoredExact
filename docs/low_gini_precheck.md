# Low-Gini Precheck

The low-Gini precheck option is currently recorded and used through the existing penalty/domain closure rows. A separate standalone LP precheck was not promoted to paper-core evidence in this round; future work should expose its LP status and basis independently.
