# Round89 second qualification: independent receipt review

**Verdict: pure mathematics and backend activation passed, but the native callback qualification is inapplicable and no real-instance screen is admitted.** This review read archived files and source only; it ran no build, test, or solver.

Root's follow-up admission allowed only `wideFixture` to set `Y_1,Y_2` upper bounds from 10 to 5. The current Micro source SHA256 is `75ed357d3d370ebcb591fb885056609a40b3f9cbee83dfd4133879ec91b819fc`, its rebuilt executable `28a4a92012628ceb2043a284e616cb0b49da1214859aa8a5419aaa22aa9bd176`. `identity_post.json` matches the nine admitted sources, unchanged main executable `8d5f0ad4a3cf588875a6a3b8ac67c5bdbb2b2df54a5fd49bea56de2034a17e8d`, and new Micro executable. The rebuild receipt shows only the Micro object and link, with no core archive rebuild. There is no separate `identity_pre.json` in this batch; `admission.md` records the pre-rebuild check and every executed command records its executable hash before/after.

Round89 `pure` and `pure-json` each exited 0 once. The independent `Fraction.from_float` oracle exited 0 once and its receipt reports 54 integer-state checks, including all 36 pairs of the submitted six-state row with genuinely rounded `γα` supports and three nonexact products. The earlier Round53 30-case and Round55 58-check passes belong to `qualification_001` and were not rerun.

One `native-six-arm` process was launched, exit 1 without timeout. Its incremental `arms.jsonl` contains exactly six `optimize_rc=0` outcomes: direct off/static/callback each `GRB_OPTIMAL`, objective 1; backend LP negative control `OPTIMAL`, objective 0 with B1 inactive; backend terminal and partial MIPs `OPTIMAL`, B1 active and audited. The original LP SHA256 `5d641a392ccba85e0957082d104db98f6f33d9a41766ed7b65e199ab8ea3a940` has the expected one-hot/two-link/h rows. Both backend MIP summary files report 10 audited rows, one pair, `PreCrush=1`, unchanged `FeasibilityTol≈10⁻⁶`, and zero numerical skips. Their Gurobi logs show root relaxation objective 0 with two integer infeasibilities, while integer objective and bound finish at 1. The LP negative control reaches objective 0 in three simplex iterations. These are solver numerical receipts, not an exact rational LP certificate.

The decisive missing event is explicit: both backend MIP summaries have `mipnode_calls=optimal_nodes=pairs_checked=reliable_rows=submitted_api_ok=0`; the direct callback arm has `fractional_nodes=submitted_api_ok=0`. Gurobi logs report user-callback activity and a fractional root, but do not imply an optimal `MIPNODE_REL` event. The direct callback toy therefore fails its declared exposure criterion with `toy callback qualification inapplicable_or_failed`. Neither `GRBcbcut` API success nor cut effect has been observed. The final failed receipt overwrote the in-progress success JSON containing direct primal vectors; the arm JSONL preserves objective/status but no primal. Source control flow shows the three direct objective and original-h checks ran before the final callback assertion, yet the raw primal cannot be independently reconstructed from this archive. Do not report an independently audited direct physical witness.

The five outer receipts total 3.830 s child process wall and 3.861 s non-overlapping wrapper wall, including 3.360 s Micro rebuild and the native command's 0.094 s child/0.109 s wrapper. Its nested 0.045 s internal elapsed is a component of the 0.094 s and must not be added. Together with `qualification_001` (0 Optimize), this batch used the full six authorized toy Optimize calls, with no retry or further parameter/source change. The file index retains the partial/failed receipts, LP and Gurobi logs; `process_post.json` reports zero active processes.

Next admission, if any, should be a separately designed and reviewed callback-exposure toy with a new lease and complete per-arm primal/callback receipts. This result is insufficient to admit a real-instance native B1 run or to claim bound, proof, or time benefit. No production-code failure is established by the absence of MIPNODE events on this tiny solved tree.
