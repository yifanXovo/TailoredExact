# Round89 native B1 amended qualification admission

Root's follow-up message admits one harness-only repair after the retained `qualification_001` failure. The signed original lease is `results/unified_exact_round89/qualification_lease.json`, SHA-256 `bb6d7a746ae0b9fb8601721dc3249fa233d9b88bedd1c353e4a448f4645e7972`; the earlier batch stopped at Round89 pure with zero Optimize and remains unchanged.

Only `tests/round89_native_ot_b1_micro.cpp` changed: `wideFixture` now sets `Y_1` and `Y_2` upper bounds to 5, matching its six inventory states 0–5. The old source SHA-256 was `24e5b6c49dfa91108e1e1f4f538cc7c9a3a0564888670a02ce4fe8ac5b82d2e0`; the new source SHA-256 is `75ed357d3d370ebcb591fb885056609a40b3f9cbee83dfd4133879ec91b819fc`. The `r` and `h` bounds remain 10. All other eight lease source hashes and the admitted main executable hash still match before rebuild. Root retains Git ownership.

Authorized sequence for this fresh directory: rebuild only `Round89NativeOtB1Micro`, check old/new executable SHA and unchanged main/core identity; run Round89 pure once under 120 s; run pure-json once and independent Fraction oracle once, each under 120 s; only if all pass, run the declared six-arm native toy once with a 120 s whole-process cap. Do not rerun the already passed Round53/Round55 tests. The total new Optimize count across both batches is at most six. Preserve all command stdout/stderr/exit/timing receipts, model/log/primal evidence and identity hashes. Stop on any further failure; no source repair or retry without a new root decision. `record_command.py` is byte-identical to the first batch's supervised command recorder (SHA-256 `cb2565e8b72dac08fc363164250fa2b31f3b21a7a8dc24f834527aeb110b886d`).
