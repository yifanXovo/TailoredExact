# GF Compact-BC Summary Audit

`scripts/audit_gf_compact_bc_summary.py` checks that paper rows do not use BPC or route-mask certificates, that controlled paper rows are single-thread fair, that `no_new_cuts` rows do not report disabled cut families as effective, and that compact-BC leaves have valid bound scopes when used.

For this round the summary audit reports zero failures.
