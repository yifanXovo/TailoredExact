# Round88 sparse OT epigraph 六臂诊断独立收据审查

**结论：六臂收据与报告一致，无发现身份、截止或矩阵计数阻断。** 仅 F2 root 的 B1/B2 有完成并经残差门禁的**数值固定 LP**目标；四个 D7 臂是 Optimize 进行中触及整臂 300 秒截止的 `unknown`，无完成目标、Work、primal 或残差。不能据 F2 的结果宣称完整方法提速，也不能把历史 D7 child 当成 ENS 保留叶。本审查只读收据与少量日志，未求解、重审大 LP 或重算 174 MB 原始文件哈希。

六臂均按预注册 F2-root B1/B2、D7-root B1/B2、D7-child B1/B2 顺序在各自目录运行。六份 `supervision.json` 的 manifest 前后 SHA-256 均为 `d431b54bf9e3b640e9c49774a10b9ad65680abf695a53388665d719a570a09b1`，最终源身份为真、Win32 Job 已指派；F2 两臂返回 `complete_optimal_numeric_lp`、未超时，D7 四臂均 `timed_out=true`、监督状态 `unknown_whole_diagnostic_deadline`、批次返回码 124。D7 的子进程 `returncode=0` 不改写监督超时判定。六份 provenance 的 LP/input SHA、支持指纹和实际 G 域同冻结资格来源一致，且新增列/行/NZ 的计划值与实际 Gurobi 增量逐臂相同：F2 B1 `11848/23259/73926`、B2 `23696/46328/193026`；D7 两域 B1 `65633/131084/444144`、B2 `131266/260943/1150085`。D7-root 上界 `2625138976942465/9007199254740992`，child 上界为其一半，B2 未跨域搬用。

F2 B1 的 `result.json` 为 `OPTIMAL`、目标 `0.5327745493720414`、Work `36.8646017287`，原行/新增行/界最大残差分别 `2.81e−13/6.27e−14/0`；B2 为 `0.5327745493720415`、Work `57.7173537811`，残差 `8.04e−14/7.38e−14/0`。两份 primal、逐行残差、求解日志及完整 result 存在；日志各有完成的 `Optimal objective`。故该固定 F2 源未观察到 B2 相对 B1 的数值界增益，B2 的墙钟、Work 与矩阵均较大。此处的 OPTIMAL 仍是原 Gurobi 参数和残差标准下的数值结果，无有理对偶证书。

四个 D7 的 `inflight.json` 均为 `phase=optimize`，有完整构建 provenance/精确新增行账本及截断求解日志，却均无 `result.json`、`primal.json`、`residuals.json`。日志末尾仍为 simplex 迭代且未见完成语句；其中的当前目标数不得当作已审计最终界，solver Work 和峰值内存也未形成结果收据。整臂墙钟依次 `300.141729/300.136953/300.127350/300.134193 s`，超过 300 的约 0.13–0.14 秒对应清理与收据，不能理解为另给求解器的时间片。F2 两臂为 `16.493765/25.211144 s`。

`batch_summary.json` 的六臂监督墙钟合计 `1242.245134 s`；批次内计 `1242.296293 s`，最外 shell 命令收据 `1242.474613 s`，三者是嵌套口径，不相加。此前资格最外 `4.416753 s` 单独报告。六臂 stderr 均为空；四个 D7 子目录没有伪造完成结果。当前只读进程查询中六个收据记录的子 PID 均不存在；这与 Job 监督和作者的无遗留进程说明相符，但并非对历史每一孙进程的独立时间点证明。

`raw_file_index.json` 有 64 条互异路径、合计 `174931454` 字节；与现存原始文件的数目、字节总量一致，排除索引自身。抽核六份监督收据与两份 F2 result 的 SHA 均匹配索引；六份行账本在索引中的 SHA/字节数与各自 provenance 一致。未重新哈希所有大文件，故索引其余哈希仅按作者归档收据引用。没有证据支持 D7 的 B1/B2 强弱、epigraph 对迭代闭包的完整运行优势或正式 ENS-C 加速。
