# Production integration and bounded qualification admission

The frozen standalone prototype13bf7a5317220ea60a283e814aefbf77245de1fd
passes all eight oracle cases and five complete fixed-witness replays with
zero Optimize. Compile5.603660s; six diagnostic processes0.328s; all normal.
U6 F .214430471758 -> .178390957548 (24 exchanges,1 relocation,43 strict moves);
D7 .300543518294 -> .271177227019 (3 exchanges,12 strict moves);
D6 .157849712515 -> .157083131103 (13 exchanges,2 strict moves).
E8/N12 unchanged. U6 G improves while penalty worsens. These are additional
fixed-witness local improvements, not actual startup or complete-MIP effects.

Admit one default-off preset research-round83-vds-equal-net-exchange (ENS-C).
It inherits R73 constructive-seeded decoded startup and all qualified R78 exact
components, replacing the R78 physical controller with the frozen R83 union
controller after the same25 decoded paths. It does not first run complete BDS
then launch a second algorithm; trajectories may differ from the diagnostic
which began at BDS's final snapshot. All paths use the same single rule.
Keep strict original-F acceptance and paid physical handoff conventions.
No new parameters, seed changes, API settings, internal time slices or defaults.
The inherited exact phase still requires metric travel for its separate cuts;
the isolated neutral operator itself does not require metric travel.

## Further allocation before new native or startup results

One new complete Release build in build/round83/v1: configure<=60s, build<=600s,
all62 CTests serial<=600s, expected about156 Optimize, ceiling180. The existing
60 tests are same-build regression controls; add structural and actual preset
CLI tests. Repeating structural fixtures here is qualification of the newly
linked production library, not a second performance realization. Check actual
native logs, legal tiny certificate, physical witness, Start vector and readback.
Reserve v2 only for an identified correctness/build/qualification failure,
preserve every earlier failure and count all calls (total qualification ceiling360).
Never repeat passing qualification without a relevant source change.

After qualification and actual-vector audits pass, exactly10 serial fresh
startup-only runs: U6,D6,D7,E8,N12, BDS-C then ENS-C on the common new binary.
Each whole cap60s with3s shutdown and59.8s external guard; total<=600s,
zero Optimize. Use original inputs from diagnostic identity, no changed T/Q/data.
Independent replay must check all25 decoded logical paths, same preclosure
witness, every physical/neutral/strict transition, and actual outer handoff.
Do not require revised and old postclosure routes to match or presume that a
larger neighborhood preserves every old benefit. Record all costs and failures.
No HGA/K1 startup-only arm: its existing paid full role is already known.

If actual startup remains promising and valid, freeze a separate limited full
comparison plan with current P-GRB/BDS-C/ENS-C/K1-R controls on U6 at3600s,
plus small repair/certificate protection roles before launching. No full run
is allocated by this document. If the actual path loses the diagnostic benefit,
analyze that result before adding more variants or runs. Any final candidate
requires further long protection and new unadapted confirmation beyond R82.
All resource allocations are experiment management, never solver decisions.
