# Round72 initial validation work: cheap startup attribution

Base: Round71 final73ce04d9a3c5031c2b5a1021523502f0a9a85168 / draft PR132.
The overall goal remains unchanged and unmet. Ordinary usage is currently
allowed with1% weekly remaining; no reset credit is authorized or used.

First perform six standalone primal-heuristic diagnostics on the original
D3/C2/D4 inputs: DS and DS-X for each, using the identical qualified Round71
binary, original parameters and uniform logical processor2/mask4. Each complete
diagnostic has a30s whole-process cap; six launches/180s maximum, zero native
Optimize calls expected. No full exact run or new confirmation is opened.
Stop further launches on any validity/deadline/configuration failure; preserve
all attempts. All startup and process costs are charged separately as diagnostics.

The source already provides method primal-heuristic, which invokes the same
paid search/verifier path and returns UB-only scope. It does not call the exact
proof engine. Full P-GRB/DS/DS-X certification comparisons on D3/C2/D4 remain
required. These real-input component diagnoses cannot substitute for those
comparisons, qualify a performance claim, or complete this validation stage.

Question: does the cross-route neighborhood change the retained initial route,
objective and actual descent trajectory on these unmeasured roles? An unchanged
startup narrows attribution; an improved startup may motivate the certificate
recovery check, but does not predict native proof performance.

Preserve all original T/Q/inventory/handling settings. Fixed24 seeds at20260626,
finite strict decoded descent and all candidate rules are unchanged. The six
standalone witnesses are independently physically checked; there is no global
certificate or lower-bound claim. Do not import these diagnostic witnesses into
future formal runs. Those runs must recompute and pay for their own startup.

Reuse the49 previously passed CTests and frozen199-file/binary identity as
inherited qualification, not new tests. No compilation or algorithm revision.
Keep Round71 outputs immutable and use separate Round72 launch, cost, witness
and trace files. After diagnostics, preserve an in-progress checkpoint; do not
call this component-only prefix a completed optimization stage.
