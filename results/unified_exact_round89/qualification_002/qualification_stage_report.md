# Round89 native B1 amended qualification — toy callback inapplicable

The root-authorized two-bound harness correction is recorded in `admission.md`. Rebuild touched only the Micro object and executable; all other lease source hashes and the frozen `ExactEBRP.exe` matched the original lease; the Ninja receipt shows no core archive rebuild (`rebuild-micro.stdout.log`, `identity_post.json`). The new Micro source SHA-256 is `75ed357d3d370ebcb591fb885056609a40b3f9cbee83dfd4133879ec91b819fc`; executable SHA-256 is `28a4a92012628ceb2043a284e616cb0b49da1214859aa8a5419aaa22aa9bd176`.

The previously passed Round53/Round55 regressions were not repeated. Round89 pure passed once. The pure-json fixture and independent Fraction oracle passed once: 54 exact integer-selector checks, including all 36 pairs for a submitted six-state row with genuinely rounded `γα` supports; three nonexact products observed. Their raw receipts and fixture are retained.

The single authorized native invocation launched all six declared toy Optimize arms; its whole process exited 1 after 0.094 s, below the 120 s cap. The three direct arms were OPTIMAL with original integer objective `h=1`; the backend LP negative control was OPTIMAL with objective 0, and the backend terminal/partial MIPs were OPTIMAL with objective 1. The exported original LP and all per-arm Gurobi logs are retained. The two production-backend MIP summaries show B1 model audit valid, `PreCrush=1`, one pair and ten audited rows; LP was inactive. However, **no optimal MIPNODE callback occurred** in either backend MIP (`mipnode_calls=0`), and the direct callback arm likewise recorded `fractional_nodes=0`. Thus there was no `GRBcbcut` submission in any arm. The original root relaxation was fractional (backend logs report objective 0 and integer infeasibility 2), but Gurobi supplied no usable MIPNODE event on this tiny solved tree. The fixture failed its declared callback exposure criterion (`toy callback qualification inapplicable_or_failed`); this is not evidence that the native B1 callback works or that its cuts improve a proof. No real-instance Optimize ran.

No native retry, parameter change, source edit, or additional Optimize followed. Across `qualification_001` and `qualification_002`, the actual Optimize count is **6/6 authorized**. In this second batch, the five non-overlapping outer command receipts sum to 3.830 s process wall and 3.861 s wrapper wall, including 3.360 s engineering rebuild and 0.109 s native wrapper. The native child records 0.045 s nested in the 0.094 s outer process and is not added again. The existing failure `qualification_001` remains separate with 3.751 s wrapper wall and zero Optimize. The final toy success receipt/primal was not emitted because the Micro assertion failed; the immediate arm JSONL, LP, backend summaries, logs, and failed receipt are preserved. Post-run identity matched and no solver/build process remained; the exclusive compute slot is released.

Further callback qualification requires a new root decision, an independently reviewed toy exposure method, and a new execution lease. No real screen is admitted by these results.
