/goal
You are working inside my local repository. There are two sibling directories:

- ExactEBRP/ : create the new project here. Do not put new source files elsewhere unless needed for scripts/results.
- Hybrid GA/ : this is the old project. It contains an existing C++ implementation, including Solver_Exact_CPLEX.cpp, and test data under Hybrid GA/testdata/Smallnetwork2/. Treat this as reference code for parsing, data generation, old CPLEX formulation, and objective/feasibility conventions. Do not break or rewrite the old project except when copying/adapting code into ExactEBRP/ with clear comments.

Main goal:
Build and optimize a C++ exact solver project for the Equity-aware Bike Repositioning Problem (EBRP). The new project must solve the same problem as the old CPLEX MILP, but should implement a tailored exact method, preferably a branch-price-and-cut / branch-cut-price portfolio, and compare it against a multi-threaded CPLEX exact solver on the same data. Do not stop until the tailored exact method has certified global optimality on at least V=8 samples and is clearly faster than the CPLEX baseline on those samples. Then run one V=10 test round and report the outcome, even if the V=10 run is still hard.

Important restrictions:
- Do not simplify the test data ranges. Do not reduce truck capacity Q, station capacities C_i, target values, weights, distance matrix, or time window to make the algorithm artificially easier.
- Do not use sample-specific patches. You may exploit mathematical structure discovered from samples, but the resulting cuts/bounds/branching rules must be generally valid for the EBRP model.
- Do not report “optimal” unless there is a certificate: either a closed MIP gap, a closed branch-price tree with exact pricing at every open node, or a mathematically equivalent exhaustive route-load column enumeration.
- Use multiple threads where appropriate. Set CPLEX Threads=N for the baseline. For the tailored method, parallelize over branch nodes, Gini-frontier subproblems, pricing problems, or portfolio methods when safe.

Problem definition:
There are V stations indexed 1..V and one depot indexed 0. Station i has capacity C_i, initial inventory Y_i^Initial, target inventory Y_i^Target, and weight w_i. There are M identical or near-identical vehicles, each with bicycle capacity Q_k. Every vehicle starts empty from depot 0, visits a sequence of distinct stations, and returns to depot. Across all vehicles, station visits are disjoint: each station is visited by at most one vehicle. At a visited station, a vehicle may pick up or drop off an integer number of bikes. Vehicle load must remain in [0,Q_k]. Station inventory must remain in [0,C_i]. A vehicle may return to the depot with bikes; those bikes are unloaded at the depot and the depot has infinite capacity. The vehicle route duration is travel time plus station pickup/drop-off operation time plus final depot unloading time, and must be <= T for every vehicle. All vehicles start empty.

For final inventory Y_i^Final, define r_i = Y_i^Final / Y_i^Target. The Gini term is
G = sum_{i=1}^V sum_{j=1}^V |r_i-r_j| / (2 V sum_i r_i)
  = H / (V S), where H = sum_{i<j}|r_i-r_j| and S = sum_i r_i.
The satisfaction penalty is P = sum_i w_i |r_i - 1|.
The objective is minimize G + lambda * P.

Required executables / CLI:
1. Run tailored exact method on one file or one instance folder:
   ./ExactEBRP --method tailored --input <path> --lambda 0.15 --T 3600 --threads <N> --time-limit <seconds> --log <logfile> --out <json>

2. Run multi-thread CPLEX baseline on one file or one instance folder:
   ./ExactEBRP --method cplex --input <path> --lambda 0.15 --T 3600 --threads <N> --time-limit <seconds> --log <logfile> --out <json>

3. Run comparison:
   ./ExactEBRPCompare --input <path> --lambda 0.15 --T 3600 --threads <N> --time-limit <seconds> --out <csv_or_json>

Each output JSON must include: status, objective, G, P, lower_bound, upper_bound, gap, runtime_seconds, nodes, columns, pricing_calls, cuts_added, route list, station operations, final inventories, and an independent feasibility/objective verification block.

Project structure suggestion:
ExactEBRP/
  CMakeLists.txt
  include/
    Instance.hpp
    Parser.hpp
    Evaluator.hpp
    Result.hpp
    CplexBaseline.hpp
    TailoredExact.hpp
    Column.hpp
    Master.hpp
    Pricing.hpp
    Cuts.hpp
    Branching.hpp
    Gini.hpp
    Logger.hpp
  src/
    main.cpp
    compare_main.cpp
    Parser.cpp
    Evaluator.cpp
    CplexBaseline.cpp
    TailoredExact.cpp
    Master.cpp
    Pricing.cpp
    Cuts.cpp
    Branching.cpp
    Gini.cpp
  scripts/
    run_compare.py
    summarize_results.py
  results/
  logs/

Implementation phases:

Phase 0: Build data layer and verifier.
- Read existing Hybrid GA/testdata/Smallnetwork2 samples using the same format as the old project.
- Reuse or adapt the old parser/generator logic. Preserve all values exactly.
- Implement Evaluator that independently recomputes route travel time, pickup/drop/depot-unload operation time, load feasibility, station feasibility, final inventories, G, P, and objective.
- Add unit tests on provided V=12 candidate text files if available.

Phase 1: Multi-thread CPLEX baseline.
- Implement the original CPLEX MILP as faithfully as possible, using Solver_Exact_CPLEX.cpp and the paper/model spec as references.
- Enable CPLEX multi-threading through CPX_PARAM_THREADS / IloCplex::Threads.
- Add logging of incumbent, best bound, MIP gap, nodes, and runtime.
- Add optional strengthened constraints that are mathematically equivalent and globally valid, but keep a switch to run the plain old formulation for apples-to-apples comparison.

Phase 2: Tailored exact portfolio.
Implement a portfolio, not a single fragile method. At minimum include these exact components:

A. Small-instance full route-load column enumeration mode.
- For V<=10 or when enumeration is proven manageable, enumerate all feasible elementary route-load columns for one vehicle under T and Q.
- A column is a complete feasible route plus integer station operations. It must satisfy vehicle load, station inventory, and duration constraints.
- Then solve the exact set-packing/inventory/equity master as a MIP. This is exact because the full feasible column set is enumerated.
- This is a size-adaptive exact mode, not a sample-specific patch.

B. Gini-frontier route-load branch-price-and-cut mode.
- Use route-load columns. The master selects feasible columns; it does not contain arc-flow or MTZ route variables.
- Master constraints: vehicle count, station disjointness, final inventory aggregation, station capacity, r_i, P linearization, and Gini handling.
- For fixed Gini cap gamma, use H <= V*gamma*S to avoid Gini fractional/bilinear expansion.
- Use this as a frontier/bounding engine. Combine it with exact weighted-objective certification via interval/box search over G or a direct exact master objective. Make the certificate explicit in logs.
- Pricing must be exact. Implement at least one of:
  1) C++ label-setting resource-constrained elementary shortest path with load and pickup-budget resources;
  2) compact single-route CPLEX pricing MIP;
  3) both, using label-setting first and MIP pricing as proof fallback.
- A restricted master LP node is certified only when exact pricing proves min reduced cost >= -tolerance.

C. Strengthened compact fallback.
- Keep a strengthened compact CPLEX/Gurobi/CPLEX-MIP fallback using globally valid cuts. It can be used to verify results, obtain incumbents, or close small instances.
- Add operation-time conservation, route-duration lower bounds, symmetry breaking among identical vehicles, and any globally valid station/inventory cuts.

Mandatory valid mathematical components to implement or evaluate:
1. Operation-time conservation:
   Since vehicles start empty and unload remaining bikes at the depot,
   operation_time_k = tau_pick * total_pick_k + tau_drop * total_drop_k + tau_drop * (total_pick_k - total_drop_k)
                    = (tau_pick + tau_drop) * total_pick_k.
   For tau_pick=tau_drop=60, operation_time_k = 120 * total_pick_k.

2. Gini cap linearization:
   G <= gamma iff H <= V*gamma*S, where H=sum_{i<j}|r_i-r_j| and S=sum_i r_i.

3. Route-load column equivalence:
   The set-packing master over all feasible route-load columns is equivalent to the original route/load feasible region when station visits are disjoint and each selected column is internally feasible.

4. Exact pricing certificate:
   In Dantzig-Wolfe decomposition, if the exact pricing oracle proves no column has negative reduced cost at a node, the restricted master LP solution is the full master LP solution for that node.

5. 3-subset-row cuts:
   For any station triple S, add sum_c floor(|A_c cap S|/2) z_c <= 1. Separate violated cuts dynamically.

6. Co-route / Ryan-Foster pair branching:
   For pair (i,j), branch on sum_{c containing both i,j} z_c = 0 or = 1. Update master rows and pricing restrictions accordingly.

7. Lorenz/Gini cuts for larger V:
   For sorted r_(1)<=...<=r_(V), H = sum_l (2l - V - 1) r_(l). Use dynamic separation if pairwise h_ij becomes too large for V around 50-100.

8. Resource-route bounds:
   Use lower bounds on route travel plus operation time to prune infeasible partial routes or columns. For small support sets, exact Held-Karp or DP lower bounds are allowed; for larger V, use MST/1-tree/nearest-return lower bounds.

Branching and search requirements:
- Prefer co-route pair branching over arc branching in column mode.
- Also allow branching on station served/unserved, final inventory intervals, and operation quantity when needed.
- Maintain a global incumbent and valid global lower bound.
- Implement deterministic logging of open nodes, best lower bound, incumbent, and gap.
- Run branch nodes in parallel if possible, but ensure correctness of shared incumbent and bounds.

Performance target / stop condition:
- Use Hybrid GA/testdata/Smallnetwork2. If no V=8 samples exist, generate V=8 samples using the existing generator and the same parameter distributions, without simplifying capacities/Q/targets/weights/time window.
- First run the multi-thread CPLEX baseline and tailored exact method on V=8 samples.
- Do not stop until tailored exact returns certified global optimality and is clearly faster than CPLEX on at least one V=8 sample; preferably test at least 3 V=8 samples and report median speedup. A strong target is >=3x speedup, but any clear and repeatable speedup with full certificates is acceptable.
- After V=8 success, run one V=10 sample with both methods and report objective, gap/certificate, runtime, and logs. If V=10 does not close, provide the best incumbent, lower bound, gap, and detailed bottleneck analysis.

Do not hide failures:
- If an attempted cut/pricing/branching method does not help, keep a short note in docs/attempt_log.md explaining what was tried, on which sample, and why it was abandoned.
- Do not report heuristic results as exact results.
- Do not stop at root LP bounds unless the integer master/branch-price tree is also closed or full column enumeration plus exact master MIP has closed.

Final required report:
Create ExactEBRP/docs/algorithm_report.md with:
- Problem statement and data assumptions.
- Exact mathematical formulation(s).
- Tailored exact algorithm flow.
- Implemented cuts/bounds/branching, with proof sketches.
- Build and run instructions.
- Test table comparing tailored exact vs CPLEX on V=8 and one V=10 sample.
- Logs/results file locations.
- Remaining bottlenecks and next optimization plan.
