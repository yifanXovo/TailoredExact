# Current state and Round 22 execution plan

Round 22 starts from live GitHub `main` commit `f123418f9ae50c7bd5d09616757cd56d18cb3906`, whose tree contains merged Round 21. The work branch is `codex/round22-engineering-certificate-dense-trajectories`. Pre-existing modified and untracked files are excluded from Round 22 staging and remain untouched.

Implemented mechanical changes are the engineering-exact certificate classifier, a versioned complete-model correctness gate, clarified independent verifier fields, full residual diagnostics, shared buffered dense CPLEX progress capture, canonical checkpoint primitives, and deterministic unit tests. The held-out suite, arm manifests, protocol, and instance hashes are frozen before production.

The first pre-freeze Stage 1 gate demonstrated that CPLEX emitted global progress only sparsely and did not emit local progress for the combined Tailored callback mask. Those nonofficial-for-final-freeze observations and their binary are retained under `attempts/pre_refreeze_753275454e066ff7`. The corrected Tailored path records the same callback-safe information at the start of its documented relaxation context, before any algorithm mutation. A fresh executable/source freeze and a complete replacement Stage 1 are required before any production matrix row.

The first attempted production Stage 2 then exposed a runner-only integrity assumption: a 391,404-event plain CPLEX trace contained 3,816 full-precision negative native-bound steps, including four isolated excursions above the old absolute `1e-7` cutoff; the maximum was `6.60548520547977e-7`, and the final record exactly matched final JSON. The failed row and the two preceding rows are excluded from the replacement production matrix and retained with the prior replacement Stage 1 under `attempts/pre_integrity_erratum_cf12a925`. The explicit erratum reports all raw fluctuations without repair and uses a fixed `1e-6 * max(1, |previous|, |current|)` material-corruption scale only for trajectory integrity. The binary algorithm and certificate semantics are unchanged; source/protocol are refrozen and all gates rerun before replacement production.

Execution is serial: clean release build and all mechanical audits; live callback and matched overhead gates; 24 existing-suite rows at 900 seconds; 24 existing-suite rows at 1,800 seconds; 18 held-out rows at 900 seconds; then only preregistered conditional 3,600-second comparisons. Every official row uses one frozen executable, exact-zero relative/absolute gaps, one thread, one fixed manifest, and dense progress. Excluded/interrupted/pre-freeze attempts remain outside official summaries.
