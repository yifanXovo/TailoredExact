# Round 61 completed research record

Base: PR #121 and remote `codex/round60-verified-candidate-native-injection`
both checked through GitHub API at `fe00f198a8b8620513017b02708315b5841c4daf`.
Local HEAD was identical. The first git transport query failed with a network
reset; independent API branch and PR reads succeeded. No later research commit.
New branch: `codex/round61-transfer-block-native-time-oracle`.
Pre-existing tracked changes in three historical result files and all untracked
build/result/manuscript files are user state, excluded from this delivery.
No applicable AGENTS.md found in repository or parent directories.

Completed: 71/72 charged launches, 2/4 native micro, 90 internal optimizer calls,
no process failures, watchdogs or over-budget runs; final CTest 38/38 passed.
The independently checked records contain 114 witness entries. PREFIX is the
uniform frozen candidate. D3 and D7 improve in long comparisons, but C2 loses a
certificate, so the new SUBMIT configuration is not promoted. A global D4
four-station inventory conflict is valid but its bit no-good does not separate
the recorded LP points; no static cut experiment was added.

Start with [final_report.md](final_report.md) and the generated
[data_appendix.md](data_appendix.md). [reproduce.md](reproduce.md) documents build,
replay, analysis and audit commands. [mathematics.md](mathematics.md) explains
algorithm invariants and scope; [cheap_time_proof.md](cheap_time_proof.md) gives
the bounded analytic conflict argument.

`protocol.json` freezes the panel, work rules and thresholds before performance.
`processes.jsonl` contains exact commands, caps and build hashes for every launch;
`optimizer_calls.csv` accounts for internal calls. All optimizers ran serially,
with compilation separated from performance. The build-freeze files distinguish
initial, route-normalized and cutoff-admission experiments. The final error-path
compatibility build is separately recorded, not substituted into earlier pairs.
Compact CSVs, `oracle_evidence/`, and `witnesses/` are committed. Large raw logs,
canonical models and preserved paired binaries stay in ignored `local_raw/`,
with paths and hashes in `local_artifacts.csv` and the executable manifest.
