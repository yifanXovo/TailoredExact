# Structural Profile Policy

`auto-diagnostic` recommends a profile from generic interval metrics only. It does not alter paper-default behavior in this round and never reads an instance name, seed, path, or known outcome.

Fresh evidence recommendation: `route_cutset_callback_limited`.
