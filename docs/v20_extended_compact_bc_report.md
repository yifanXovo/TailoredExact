# V20 Extended Compact-BC Report

The one-thread V20 mini-suite produced final JSON for all six V20/M3 stress rows. `tight_T_seed3101` certified. The remaining five rows are noncertified at the bounded 300s setting.

The exact pipeline still gives materially stronger gaps than same-budget single-thread plain CPLEX on all compared V20 rows, but the smaller one-thread compact leaf budget means high_imbalance_seed3202 did not reproduce the earlier multithread/longer-leaf certificate in this round.
