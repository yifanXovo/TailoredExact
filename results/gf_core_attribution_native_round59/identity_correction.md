# Diagnostic identity correction and remaining budget

The initial Round50 diagnostic entry used its historical V0 flags. Current
K1 inherits additional flags from paper-gf-tailored-bc before applying its
overrides. The advertised inactive-family list is therefore not authoritative:
Gini spread, required movement, global handling, ratio-band tightening,
transfer-subset capacity and explicit penalty-domain bounds are present.
No stable behavior is changed here. `--round59-current-f0` mirrors the
inherited model flags only inside the research diagnostic entry.

`current_f0_identity.json` verifies byte equality against actual K1-S initial
canonical files for D2, D3 and corrected D4. Build-only identity checks open
no optimizer and are not performance runs. The first six LP and three MIP
diagnostics remain in the process ledger (runs 41–49); their scope is
legacy-harness diagnostic, excluded from current-model attribution. The
reserved D3/Pool directory explicitly records that no process was launched.

Remaining design at process 49: six current compact/F0 root LP runs; eight
current F0/monitor/static/pool solves on D3/D4; two MIPFocus=1 solves on
D3/D4; four same-U startup-exhaustion compact/F0 solves on D6/D7; eight
confirmation full-instance runs; optionally two narrow same-HGA-U solves
on D2. Maximum total 79. No long extensions, clean timing repeats or dynamic
callback matrix. Native pool needs no user callback; historical dynamic
isolation is audited separately and cannot establish current performance.

The independent search hypothesis is fixed before current matched results:
native primal emphasis (MIPFocus=1, no starts/hints) may shorten delayed
feasible-route discovery on D3, at the cost of bound progression. D4 is the
positive/protection control. Legacy diagnostic D3 reached its final UB only
after tens of seconds and then had a long proof tail; this gives a reason
to test one setting, not to search a parameter grid. This is a solver-policy
diagnostic, not a new mathematical contribution. It will not be combined
with cuts absent independently favorable evidence.

Earlier D2/D3 full screening overlapped some diagnostic compilation (never
another optimizer). Wall-time attribution there is descriptive; no clean
causal timing claim is made. Deterministic Work, domain/model records and
the unconfounded corrected D4/remaining runs are reported separately.

## Final compact-origin audit (supersedes the optional remaining plan above)

At process 68, the supposedly compact arm was found to retain connectivity
flow, cutoff-derived domains and the F0 exhaustive-block removal even when
strengthened=false. Its results remain useful **pack-removal ablations** but
must not be called original compact. Raw arm labels are preserved; derived
tables explicitly rename the arm. No internal cut/search pairing is affected.

`--round59-original-compact-sha256` resets model options to plain defaults,
preserves the original exhaustive-block policy, first builds the unrestricted
origin, and fails unless its SHA256 matches the official P-GRB export. It
then applies the frozen interval and cutoff. `literal_original_identity.json`
records successful build-only origin and current-F0 audits for D3/D4.
Restricted-origin row comparison shows added gamma/cutoff rows and associated
G/product upper bounds; it does not inherit connectivity flow or tailored
domain propagation. Both arms of the new formulation pair use one new binary.

Final remaining budget: four literal-original/F0 solves on D3/D4 and eight
confirmation runs, for 80 charged processes total including three micro
correctness runs and the failed initial CLI attempt. D7 pack-removal solve,
optional D2 narrow-HGA pair and all long extensions are omitted. D6 and D7
same-U F0 diagnostics after HGA exhaustion are both retained. This is an
identity correction, not a revision of either internal candidate rule.
