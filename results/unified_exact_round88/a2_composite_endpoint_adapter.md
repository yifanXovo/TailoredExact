# Round88 A2 composite penalty endpoint adapter: static preparation

Status: the composite oracle's **six no-Gurobi microtests passed** on the frozen source and test hashes. The separately named native endpoint adapter and its pure-logic test source are now written but **have not been executed**. No D6/E8/S12 composite endpoint diagnosis has been launched. Frozen linear flow/probe modules, C++, prior negative diagnostics, and runner files remain unchanged.

## Qualification of the composite oracle

`qualification/a2_composite_penalty/microtest_001.log` and `.json` retain the full command `D:/msys64/ucrt64/bin/python.exe tests/round88_quantity_composite_test.py -v`, Python 3.12.7, UTC launch time, frozen source SHA256 `dccbf59fa462a25308419f49a719f59d0d9ef514f917ab377b9215ffdcdcd898`, test SHA256 `932cc1f4a47c25a8c08195ce8d4a0208062eb6f361ff36a892c5f394d213149e`, exit 0, 6/6, and outer wall 0.2646866 s (test runner 0.145 s). The 216 independent small inventory enumerations, independent full arc/primal/residual-potential reconstruction, exact marginal-fold tests, and over 1,000 cancellation combinations are part of those six tests. No failure or code repair was needed in the executed run; the earlier static reviewer corrected one six-unit marginal test expectation before execution.

## New endpoint boundary

`scripts/round88_quantity_composite_probe.py` imports the new composite module explicitly. It reuses the qualified frozen `round88_quantity_probe.py` input parser, 1-based/depot route mapping, source witness identity checks, exact budget-floor comparison with the actual C++ travel receipt, physical prefilter, complete primitive-line scanner, `cpp_verify`, receipt identity verification, and Windows kill-on-close Job. It checks the native binary, input, witness, frozen flow, frozen old probe, and composite source hashes before launch and again in the supervised child. The supervisor records its own hash before and after the child, and requires all hashes stable for a complete status. It does not patch either frozen module, override `__file__`, or silently replace the old linear gradient.

At every round, the adapter recomputes the composite proxy from the current C++-verified inventory and routes. Records distinguish the Gini-only local gradient, exact full-penalty `phi` network cost relative to original stock, proxy delta relative to the current point, integer arc/flow/potential certificate, and the **original C++ F**. It checkpoints the complete oracle certificate before scanning the line, so a timeout during later C++ calls retains that work. Candidates failing the internal physical check are logged and never sent to C++; each surviving primitive point is sent through the frozen `incumbent-import-test` importer. The importer receipt must identify the exact candidate route, integer inventory and input, verify all original physical/objective fields, and report a finite F. Only strict original F decrease moves the endpoint; equality stops on no verified improvement. The complete fixed-route iteration continues until the same mathematical terminal conditions as the prior A2 probe, without an internal seconds/work/round quota.

`supervise` is the only intended entry: preflight, child startup, every C++ subprocess, oracle, JSONL/fsync, and final result all share one external 120-second case deadline. A Win32 Job is assigned before a ready flag allows the child to call C++; job close on timeout or exception kills the child and its native descendants. Timeout, identity drift, inapplicable domain, and invalid evidence retain distinct statuses; partial child result/events remain on disk. Inapplicability additionally requires the expected child exit code 2 and matching method/case identity; a nonstandard crash after a `domain_*` write is invalid. The supervisor records complete wall cost and process status. It does not claim that a timed-out case is comparable or improved.

The fixed research cases remain D6, E8, S12 with frozen startup witnesses and route parameters from `a2_endpoint_probe_contract.md`. If later admitted, each would run **once** in that order, each in a new output directory with a full 120-second whole-process limit. Example command shape (not yet authorized or run):

`D:/msys64/ucrt64/bin/python.exe scripts/round88_quantity_composite_probe.py supervise --case D6 --out-dir results/unified_exact_round88/a2_composite_endpoint_diagnostic_001/D6 --whole-process-limit-seconds 120`

The same shape applies to E8 and S12 with unique output directories. These are offline mechanism probes, not an ENS-C strategy switch or speed claim. Before any of them, the new adapter's pure tests, a minimal real C++ receipt check, independent static review, source hashes, resource lease, and an explicit root admission must all be complete. If the three fixed endpoints again offer no verified original-F gain, this variant stops under the accepted proposal.
