# Round78 production integration and qualification admission

The completed fixed-witness diagnosis provides a structural benefit on D6/D7,
with all candidate and original witness checks passing. Admit production
integration as one uniform default-off preset:
research-round78-vds-balanced-descent (BDS-C). Inherit JDS-X's25 paid current-run
seed trajectories, then execute the same R76 strict closure / R78 neutral block
alternation. Do not import diagnostic witnesses. The VD-S exact formulation,
cover, gates, native settings and original physical/objective meaning remain.
Add the explicit preset to all existing entry/trace/metric/outer-start guards.

The production module owns the controller, so both diagnostics/tests and the
actual CLI call the same code. Trace only when an output path is requested;
all overhead is charged. Stop on zero F, declared joint exhaustion or the sole
whole-run deadline. On a prediction/physical mismatch retain the prior verified
witness with an explicit failure flag; stage validity requires no rejection.
No internal seconds/Work/candidate cap. Keep the existing outer1e-10 incumbent
threshold: a neutral-only final route is not promoted solely for duration.
This choice preserves the outer handoff contract; record it rather than claim
every lower-duration route was submitted. A selected F improvement of<=1e-10
may likewise retain the prior starter as in R76.

One new qualification revision: configure<=60s, build<=600s, serial CTest<=600s,
target60 tests /<=170 actual Optimize (inherited129 plus new real CLI18 expected).
The structural test compares every placement with independent full verification,
tests actual controller zero/deadline and a neutral-plus-strict trajectory.
The real BDS-C CLI must numerically certify tiny5/24, complete25 seeds, emit
actual balanced-controller evidence, and submit a checked complete native Start.
Use the inherited same-binary JDS-C/JDS-X tests as controls; no duplicate extra
micro solves. One additional revision is reserved only for a concrete failed
qualification defect, with all failures/costs retained and no overwrite.

After qualification and actual native-vector audits pass, admit ten fresh
startup-only processes: D3,C2,D4,D6,D7, each JDS-C then BDS-C, each<=30s,
total<=300s, zero Optimize. Freeze commands/build/input/settings/affinity before
launch. Check all25 underlying paths, every physical transition and actual
handoff. Stop after a validity failure. These are startup/mechanism screens,
not certification-time comparisons. Reuse frozen thresholds for later primary
comparisons; do not change them based on startup observations.

No complete1200/3600/7200s panel or confirmation is admitted by this file.
After the screen, declare only an informative fresh full comparison if warranted,
including required P/K1 controls and whole end-to-end costs. Persistent full
failure should move the next question to core representation/organization.
Publish one substantive R78 draft PR after its admitted evidence closes.
