# R86 prelaunch preparation notes

Four inputs were generated once after recipe freeze2d770ba01. No optimizer
has launched, no input regenerated and no candidate/source/build changed.
Reader validation1 passed37 checks in.731240900s, including21 old run replays
and84 old checkpoints; this is offline validation, not new native qualification.

Review then corrected a Windows backslash Git-show path in the new prospective
identity audit. A v2 check exposed the inherited CitiBike generator's Git LF
versus local CRLF bytes. Its local bytes exactly match the preregistered hash;
Git/local text differs only by CRLF. The correction is restricted to that
inherited file, preserving exact raw equality for all new generator/plan/recipe
files. The failed v2 precheck and prior successful receipt remain retained.
No scientific input or prior report was altered. The plan's generator-byte
freeze is implemented by committed working-byte hashes plus this explicit
inherited-source line-ending relation, not a claim of raw-byte Git equality.

One read-only rg call used a Windows wildcard path and failed; the corrected
--glob search completed. This is unrelated to native validity or performance.
