# Canonical interval models are large deterministic solver inputs. They remain
# local with hash inventories; all result JSON and exact evidence ledgers are
# published. Recreate models by rerunning the frozen commands.
smoke_runs/*/external/models/
diagnostic_runs/*/external/models/
confirmation_runs/*/external/models/
