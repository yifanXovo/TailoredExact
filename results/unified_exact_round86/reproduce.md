# R86 reproduction and closed-producer guards — completed evidence

All12 originals, generator, offline validators, audits, summary, package and
plots have CLOSED. The driver session88121/PID45336 exited0; all49 Optimize
calls returned. Never poll that closed session or relaunch any original producer.
The paragraphs describing producer order below are provenance, not pending work.
`campaign/driver_completion.json` and `stage_decision.json` are authoritative.

Recipe frozen2d770ba0163329794edc918a8ccad1735ed50b12 before generation.
round86_generate.py ran ONCE; generation.json records all four identities and
structure/overlap. Do not regenerate, replace or overwrite those inputs.
New replication needs a separate declared output root and allocation.

Use Windows, D:/msys64/ucrt64/bin/python.exe with PYTHONUTF8=1, Gurobi13.0.2
and unchanged build/round83/v1 binaries. round86_offline_validate.py has two
successful37-check receipts plus one prelaunch Git-EOL precheck failure;
all are offline, zero Optimize. v2 is the accepted driver-bound version.
Do not rerun qualification or count inherited R83 native calls again.

After committing all source/input/tooling/plan bytes, round86_research.py
runs the12 declared arms ONCE with four original compact reference exports.
Every launch, common deadline, input/settings/model identity and observation
is recorded. A validity failure stops subsequent launches without rerun.
Nullable U/gap remain null; a final-only route becomes available after exit.
Read-only monitor/checkpoint scripts never produce formal performance points.

Only after all original native processes close, run round86_analyze.py,
round86_mechanism.py, round86_confirmation_audit.py, round86_summarize.py,
round86_package.py in that order, ONCE each. Then separately capture
--verify-only stdout and verify each bundle member again. No source/build
mutation before source-bound evidence is packaged. The inherited qualification
and source bundles are R83 hash-bound bytes, not new costs.

Use existing build/round81/plot_env/Scripts/python.exe for round86_plot.py;
inspect all four PNGs and retain SVG/checkpoint bytes. No heavy audit,
compression, plot or build during a native run. One independent draft PR
against R85 follows evidence review, regardless of performance direction.
Publishers preserve exact Git commits/trees and use non-force updates;
binary creation-SHA acknowledgments are distinct from full remote readback.

## Completed verification and source/build provenance

Campaign, mechanism and confirmation audits all pass. The summary preserves12
endpoints/57 checkpoints;7 actual full Starts are accepted. Thirteen bundles
contain15277 byte-checked members and all four figures passed visual review.
The separate review-only command already ran successfully once:

```powershell
Set-Location 'E:\codes\ExactEBRP-round66'
$env:PYTHONUTF8='1'
& 'D:\msys64\ucrt64\bin\python.exe' scripts/round86_package.py --verify-only
if ($LASTEXITCODE -ne 0) { throw 'Bundle verification failed' }
```

It checks archive bytes without extraction, native optimization or replacing
the frozen audits. `delivery_verify_only.json` retains original stdout. There
is no need to run it again merely to reproduce this reported result.

`campaign/identity.json` contains all12 exact launch commands and4 reference
commands,274 source hashes, input hashes and executable/reference hashes.
Original native commands include output paths, logs, observer receipts, full
Start artifacts and the same global deadline/affinity guards. Do not replace
them with a shortened timing command when reproducing the experiment.

Source is qualified commit4496078f25c0cdad1cf7a5c39835fd23121e8978. R83
`qualification_v1.json`, `scripts/round83_qualify.py` and the inherited qualification
bundle retain configure/build commands, MinGW/UCRT compiler identity and all62
tests/165 calls. `inherited_evidence.json` binds their exact bundle hashes and
the measured-source-byte archive. Some qualified Windows source bytes have CRLF
where Git stores LF; that preserved archive is the exact measured source input.
See R83 `production_commit_byte_check.json` for the original normalized relation.
The R86 Citi inherited source has its own disclosed LF/CRLF check; all newly
generated R86 files are bound by raw-byte SHA256 without normalization.

Native runtime uses Gurobi13.0.2, with PATH containing D:/msys64/ucrt64/bin and
D:/gurobi1302/win64/bin. Compiler/solver binaries and licenses remain local and
hash-bound, not distributed credentials. The measured executable is
build/round83/v1/ExactEBRP.exe, SHA256
25b7ec3a6d89d9f0f921c2984fbb1d9876f36f67e617af144275c88b44e6255e;
reference binary SHA256
f9456c99759c9e9e62dcf19cc13300ce55f6d30d616d0547e75026a1d68fa3c3.

## Independent reproduction boundaries

Raw original trees remain in `campaign/local_raw`; compact bundles preserve
all logs, models, observations, witnesses and Start vectors. Source paths in
original receipts are absolute Windows paths. For relocated offline replay,
verify member bytes first, retain original receipts, and use a separate path map
or isolated original-layout copy. Cross-OS live replay is not demonstrated.
Original checkpoint availability times must not be replaced by replay time.

A fresh performance realization needs a new empty output namespace, a finite
plan and controls, bound source/compiler/solver/settings, full observer/affinity
guards and separately preserved receipts. Do not remove duplicate-run guards
or overwrite this evidence. A new build is not automatically this binary or a
matched timing realization. No new replication is allocated by this document.
Final adoption uses the single preset in `unified_method.md`, with the supported
scope and tradeoffs in `goal_evidence_scope.md`. Main/default remain unchanged.
