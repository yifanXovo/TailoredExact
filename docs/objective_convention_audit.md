# Objective Convention Audit

`scripts/audit_objective_convention.py` checks final JSON metadata for input paths, convention notes, verifier status on certified rows, sealed compact-BC provenance, and absence of BPC/known-UB/archive contamination.

The round audit reports zero failures. The handling-time model-generation test also confirms that the aggregate global handling row charges pickup variables only under the current verifier convention.
