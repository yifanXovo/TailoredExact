# ENS-C/R83 生效计数参数只读审计（2026-09-27）

范围：当前主目录源码的 R83 `research-round83-vds-equal-net-exchange`，以及 Round88 runner 的 ENS-C 命令；只读，不改变已冻结算法与既有实验结论。`src/main.cpp:264–268` 将 R83 递归建立在 R73/R71/R70/R68/R67，再经 R65 和 `configurePaperK1AmSfOverrides`；`scripts/round88_a1_g3.py:157–164` 的 ENS-C 命令未覆盖以下两个 compact 参数。`round55-vd-p` 是 R67 设置、R83 沿用的区间 MIP 策略；其 `subset_duration_big_m="off"` 关闭的是另一个历史枚举族，**没有关闭**下述 `compact_bc_support_duration_cuts`。以下“生效”以此默认实际路径为前提；显式另传 CLI 可改变选项，不能混称同一方法。

**实际导出链核实。** `PaperK1AmSf.cpp:110–113` 设 `frontier_execution_mode="external-gini-tree"`、调度 `round31-nonblocking-native-bound`；`main.cpp:13019–13099` 据此进入 `solvePaperExternalGiniTree`。其每叶 `ensureArtifact`（`PaperExternalGiniTree.cpp:3456–3565`）以 R83 的 `options` 解析 `round55-vd-p`，设置 `spec.strengthened=true`、`interval_restricted=true`，然后 `writeCanonicalCompactModel(instance, options, …, spec)`。该函数在 `CplexBaseline.cpp:4337–4359` **直接复制** `SolveOptions model_options=options` 并调用 `writeCompactLp`；仅当 `spec.sparse_family_removal="triple-support-duration-cover"` 时才把最大支持大小改为 2，而 `round55-vd-p` 的解析结果为 `none`（`Round50IntervalMip.cpp:162–169`）。因此 R83 此路确实把 `support_duration_cuts=true, max_size=3, max_subsets=50000` 交给 `CplexBaseline.cpp:1910–1969` 的写行循环。`applyResourceAdaptiveCompactOptions` 的唯一调用在 `CplexBaseline.cpp:4955–4957` 另一条 compact 求解支路；它的 `diagnostic-minimal`、`resource-adaptive` 或静态族关闭选项可在那里禁用支持行，**不在 Paper 外部树的上述 canonical export 调用链中**。这一步区分了真实生效与单看配置字段；现有 ENS-C runner 也未传改变模型大小/静态族的 CLI 覆盖。

| 参数/机制 | 真实调用与效果 | 分类及建议 |
| --- | --- | --- |
| `compact_bc_support_cut_max_size=3` | `PaperK1AmSf.cpp:79`；`CplexBaseline.cpp:1910–1969` 只取二站、三站支持集合。rank≤3 定义有限切割族，未声称全部子集。 | ① 数学族边界。保留并明确披露其覆盖范围。 |
| `compact_bc_support_cut_max_subsets=50000` | `PaperK1AmSf.cpp:80` 真正传入 LP writer；`CplexBaseline.cpp:1917–1921` 在已考虑 50,000 个集合后不再写行。它是**静态行数截断**，不是时间/Work 限额，也不是 rank≤3 的数学必然值。`CplexBaseline.cpp:3763–3775` 的估算亦用此 cap。D7 audit 的 `n=50, vehicles=4`，完整 rank2+3 为 `4[ C(50,2)+C(50,3) ]=83,300`，故此路径上 cap 必绑定：按 `k` 先 pair 后 triple 的循环，前两车各 20,825 个，第三车 8,350 个（其中 triple 7,125），第四车零个，合计 50,000 个。每个被考虑集合都写一条 cover 或 conditional 行。**循环未在 cap 后 break，且 triple 下界作为 lambda 实参先求值**，因此 cap 限制模型行，不限制全部 83,300 次集合/下界计算。 | ② 可解释但任意的模型强度/内存计数参数；不是 ③ 禁止的资源服务切片。它造成车辆编号依赖。后续若追求去参数化，可定义并完整枚举 rank≤3（D7 多 33,300 行，成本可能大增），或证明安全支配/效力准则后统一筛行；先计实际建模与 LP/证明成本，不静默改 R83。 |
| `compact_bc_domain_propagation_mode="iterative"`, `...rounds=2` | `PaperK1AmSf.cpp:76–77` 设置，`main.cpp:2380–2392` 归一化，结果也输出这两个配置值。但在当前 `src/` 与 `include/` 中该 mode/rounds 无用于更新变量域的循环：`CplexBaseline.cpp:5095–5098` 的 completed 仅在非 off 时写 `min(1,max(1,rounds))=1`。LP writer 另有一次性的 penalty/movement 等安全域收紧，不能把它们表述为本参数驱动的“两轮迭代”。`paperK1AmSfActiveFamilies()` 仍列 `iterative_domain_propagation`，是配置/标签而非第二轮执行证据。 | 当前值属**未生效配置与过度概括的结果标签**，不是实际轮数预算。先修文稿与 provenance 用语；若以后真做传播，应以整数上下界单调收紧到无变化的固定点为自然停止条件，记录每轮变化、正确性和成本，不以“2轮”冒充闭包。无需据此否定现有 LP/证明。 |
| decoded seed 数量与 pass | `main.cpp:307–321` 经 R70/R71 设 `decoded-descent-interroute` 和 24 个随机种子；R73 添 1 个经物理验证的构造顺序，故 ENS-C 为 24+1。`HgaTgbcRunner.cpp:107–146` 拒绝 decoded 模式的 fixed generation，`HybridGA.h:1309–1379` 对每个种子反复做 pass，直到该结构化候选集穷尽、全局截止或独立验证的零值；没有固定 pass 数。每个未成功 pass 解码全部已生成候选，成功后立即重建下一 pass。`no_improve_generation_limit=2000`、`max_time_seconds` 及 `iterations=10` 虽作为字段/构造参数出现，decoded 分支不走 generation 循环；compaction mode 1 的 `nGreedyLU_RA_compact_full` 明确 `(void)max_iter`。局部时间只见统一 `processWorkRemainingSeconds` 传入绝对全局 deadline，不是单独服务份额。 | 24+1 是 ② 明示的多起点算法选择，不是 ③ 内部时间/Work 切片；各 pass 依靠有限排列/整数操作状态与严格 `1e−12` 改善而终止。全局 deadline 中断须照已有 `decoded_descent_complete=false` 报告，不得称穷尽。 |
| decoded 邻域内的 top-8/top-6 | 外层 `HybridGA.h:1220–1306` 根据执行前缀、角色与尾部供应/需求生成有限 guided route moves，按 proxy **排序但不截断/拒绝**；`run_decoded_descent` 不调用那个有 proxy 提前 break 的普通 `improve_once_guided`。但解码器 `HgaTgbcGreedy.cpp:601–602,1682–1771` 对每条路线枚举全部可行原子改量并保留最优 8 个用于本地组合，再保留最优 6 个本地候选用于跨车组合。单一原子最优仍从全部枚举取得；两个 top-k 限制的是**复合邻域范围**，不是 pass 配额。`tryResidualPairUnitPolish`、`tryRouteExactNeighborhood`、`tryPairExactNeighborhood` 中看似有 top-k、0.1/0.4 秒或节点限额，但入口立即 `return false`，不能归为当前 active。`set_decode_cache_max_entries(200000)` 仅满时清缓存，不跳过候选。 | top-8/top-6 属 ② 固定启发式邻域选型，需在论文中承认“该有限邻域耗尽”，不称所有 route/quantity 邻居耗尽；并非机器速度触发的 ③。若替换成无计数族，应先给支配证明或全组合实验及成本，不能预设等价或更快。 |

本审计没有发现上述 decoded descent 的隐藏固定 pass 数或单独 Time/Work 配额；发现的计数限制主要是**支持行截断**与**复合候选 top-k**。前者会改变不同车辆所得的模型行，后者改变解码邻域，两者都应作为当前 ENS-C 的真实算法参数报告。`domain_propagation_rounds=2` 不应被当成已执行两轮，也不应作为既有证据的性能解释。以上仅是调用链静态结论；未重扫庞大 journal、未跑模型/测试，也未修改冻结源码。
